substitutions:
  _IMAGE: asia-southeast1-docker.pkg.dev/seolleyeon-final/seolleyeon-avatar-repo/seolleyeon-avatar-worker:small-face-source-20260727-snapshot

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - lib/ai_recommend_model/avatar_generation/Dockerfile
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}
